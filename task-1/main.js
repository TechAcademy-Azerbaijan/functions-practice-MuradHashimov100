const prompt = require('prompt');
prompt.start();


prompt.get('input', function (err, result) {
  
    // Write code here
   n ;
      let a,b,c;
      // debugger
      a = parseInt( n / 100);
      b  = parseInt(n % 100 / 20);
      c = parseInt(n % 100 % 20);
      console.log(a,b,c);
      console.log(a * 100 + b * 30 + c * 2);
      return n;
    }
    kompdisk(297);
  
});
