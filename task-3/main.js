const prompt = require('prompt');
prompt.start();


prompt.get('input', function (err, result) {
  
    // Write code here
  function arrReverse(arr) {
    for (i=arr.length-1; i>=0; i--) {
      console.log(arr[i])
    }
  }
  let arr1 = [ 'p0', 'p1', 'p2' ];
  arrReverse(arr1);
  arrReverse(arr2= [92846,96841,33582,25998]);
  
});
